// prompts.js — 中文 AI 提示词模板

const DIGEST_PROMPTS = {
  overviewSystem:
    '你是一位资深知识提炼助手。用户会给你一段视频字幕文稿，请用简体中文提炼结构化内容，帮助用户快速掌握视频核心价值。',

  overviewUser: (text) =>
    '请分析下面的视频文稿，并输出严格的 JSON 对象（不要输出 JSON 以外的任何内容），字段如下：\n' +
    '{\n' +
    '  "summary": "一句话概括视频主旨，不超过 60 字",\n' +
    '  "keyPoints": ["核心要点1", "核心要点2", ...]（3~8 条，每条不超过 40 字）,\n' +
    '  "chapters": [{"title": "章节标题", "start": 起始秒数, "end": 结束秒数, "points": ["该章节要点"]}]（按时间顺序覆盖全文）,\n' +
    '  "quotes": [{"time": 秒数, "text": "值得记住的原话，不超过 60 字"}]\n' +
    '}\n' +
    '要求：chapters 的 start/end 必须是数字秒数；quotes 的 time 必须是数字秒数。\n\n' +
    '文稿如下：\n' +
    text,

  // 长文稿分段摘要（第一阶段）：把长视频切成若干片段，各自提取带时间戳的要点，
  // 再把这些要点作为素材交给 overviewUserFromNotes 汇总。目的是让单次请求的输入/输出
  // 都保持较小，避免服务端长时间无输出而被网关（常见 60s）切断。
  chunkSystem:
    '你是一位资深知识提炼助手。你的任务是为一个长视频文稿的某个片段提取要点，这些要点将被用于后续汇总，因此必须忠实、具体、带时间戳。',

  chunkUser: (text, idx, total) =>
    '下面是视频文稿的第 ' + idx + '/' + total + ' 部分。请提取这部分的核心内容。\n' +
    '输出要求：\n' +
    '1) 每条要点独立一行，以 [起始秒数] 开头，例如：[123] 讲了什么\n' +
    '2) 共 5~12 条，每条不超过 60 字\n' +
    '3) 保留关键数据、结论、人名、术语、时间节点；不要写"本部分介绍了"这类套话\n' +
    '4) 只输出要点本身，不要加小标题、不要加 markdown 符号\n\n' +
    '文稿片段：\n' + text,

  // 长文稿分段摘要（第二阶段）：基于各段要点汇总出最终结构化概览
  overviewUserFromNotes: (notes) =>
    '下面是这个视频的**分段要点摘录**（每条开头的方括号内是起始秒数）。请基于这些要点，' +
    '输出严格的 JSON 对象（不要输出 JSON 以外的任何内容），字段如下：\n' +
    '{\n' +
    '  "summary": "一句话概括视频主旨，不超过 60 字",\n' +
    '  "keyPoints": ["核心要点1", "核心要点2", ...]（3~8 条，每条不超过 40 字）,\n' +
    '  "chapters": [{"title": "章节标题", "start": 起始秒数, "end": 结束秒数, "points": ["该章节要点"]}]（按时间顺序覆盖全文）,\n' +
    '  "quotes": [{"time": 秒数, "text": "值得记住的原话，不超过 60 字"}]\n' +
    '}\n' +
    '要求：chapters 的 start/end 必须是数字秒数；quotes 的 time 必须是数字秒数；' +
    '时间戳直接引用要点中给出的秒数，不要凭空编造。\n\n' +
    '分段要点如下：\n' +
    notes,

  explainSystem:
    '你是一位耐心的学习助手，用简体中文解释用户从视频文稿中选中的内容，讲清楚含义、背景与关键信息。',

  explainUser: (selected, context) =>
    '以下是用户从视频文稿中选中的内容：\n「' + selected + '」\n\n' +
    '相关上下文：\n' + context + '\n\n' +
    '请用简体中文解释这段话（200 字以内，直接输出解释内容）。',

  polishSystem:
    '你是一位文字润色助手，只做润色，不改动原意，不添加原笔记中没有的信息。',

  polishUser: (text) =>
    '请将下面的学习笔记润色得更通顺、更精炼、更有条理（保留所有关键信息，不超过 120 字，直接输出结果）：\n\n' + text,

  translateSystem:
    '你是一位专业字幕翻译。把用户提供的视频字幕翻译成简体中文，口语自然、保留原意，只输出译文，不要加任何解释。',

  translateUser: (text) => '请将以下视频字幕翻译为简体中文：\n\n' + text,

  testUser: '请只回复两个字：成功'
};
